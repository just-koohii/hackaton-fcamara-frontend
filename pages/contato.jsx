import { PureComponent } from 'react';
import { Typography } from '@material-ui/core';

export default class Contato extends PureComponent {
  render() {
    return (
      <Typography align="center" variant="h1">
        Você está na tela Contato!
      </Typography>
    );
  }
}
